{
  "name": "openmon",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run check:loc && vite build",
    "preview": "vite preview",
    "check:loc": "node scripts/check-loc.mjs"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
